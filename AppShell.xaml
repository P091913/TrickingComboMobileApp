<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:TrickingApp"
    x:Class="TrickingApp.AppShell">

    <FlyoutItem Title="Tricking">
        <ShellContent Title="Add Trick" ContentTemplate="{DataTemplate local:AddTrickPage}" />
        <ShellContent Title="Combo" ContentTemplate="{DataTemplate local:CreateComboPage}" />
        <ShellContent Title="View" ContentTemplate="{DataTemplate local:ViewCombosPage}" />
    </FlyoutItem>
</Shell>
