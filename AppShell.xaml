<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:TrickingApp"
    x:Class="TrickingApp.AppShell">

    <FlyoutItem Title="Tricking">
        <ShellContent ContentTemplate="{DataTemplate local:AddTrickPage}" Icon="dotnet_bot.png" />
        <ShellContent Title="Create Combo" ContentTemplate="{DataTemplate local:CreateComboPage}" />
        <ShellContent Title="View Combos" ContentTemplate="{DataTemplate local:ViewCombosPage}" />
    </FlyoutItem>
</Shell>
